# Load libraries
library(ggplot2)
library(plyr)
library(dplyr)
library(plotrix)
library(reshape2)
library(broom)
library(tidyr)
library(stringr)

# shortcuts for ggplot2
rx = theme(axis.text.x = element_text(angle = 90, hjust = 1))
